import { TestInput } from "./Parser.spec";
let expect = require('chai').expect;

export default class TestUtil {

    static readTestFiles() {
        return [];
    }

    static getResultChecker(test, done) {
        return undefined;
    }
}